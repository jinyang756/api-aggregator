version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    restart: always
    environment:
      - FLASK_APP=backend/app.py
      - FLASK_ENV=production
      - PYTHONPATH=/app